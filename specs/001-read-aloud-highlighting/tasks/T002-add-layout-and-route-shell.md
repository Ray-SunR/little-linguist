# T002 Add base layout and route shell

**Phase**: Setup
**Depends on**: T001

## Task

Add the base App Router layout and the reader page route shell.

## Acceptance Criteria

- `app/layout.tsx` exists with a minimal layout wrapper
- `app/reader/page.tsx` exists and renders a reader shell component placeholder
- The route compiles without runtime errors (no missing imports)

## Instructions

1. Create or update `app/layout.tsx` with a minimal layout scaffold.
2. Create `app/reader/page.tsx` and render the reader shell component.
3. Keep the page minimal; no styling beyond required structure.
